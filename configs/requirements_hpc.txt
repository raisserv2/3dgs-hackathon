# HPC/A100 dependencies for the hloc + COLMAP pose-estimation pipeline
# Install inside the conda env you use on the server.

# Core ML stack
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124

# Pose estimation / SfM
pycolmap==4.0.4
hloc @ git+https://github.com/cvg/Hierarchical-Localization.git
opencv-python==4.13.0.92

# hloc runtime dependencies
kornia==0.7.2
h5py==3.16.0
scipy==1.15.3
matplotlib==3.10.8
plotly==6.6.0
tqdm==4.67.3
gdown==5.2.1

# Useful utilities
numpy==2.2.6
Pillow==12.1.1
PyYAML==6.0.3
requests==2.33.1
